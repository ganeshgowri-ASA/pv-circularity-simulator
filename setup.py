"""
Setup script for pv-circularity-simulator.

This file exists to enable editable installs for development:
    pip install -e .

For distribution, use pyproject.toml instead.
"""
from setuptools import setup

if __name__ == "__main__":
    setup()
