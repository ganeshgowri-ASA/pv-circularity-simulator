[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --cov=pv_simulator
    --cov-report=html
    --cov-report=term-missing
    -v
filterwarnings =
    ignore::DeprecationWarning
